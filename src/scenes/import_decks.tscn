[gd_scene load_steps=4 format=2]

[ext_resource path="res://scenes/transition.tscn" type="PackedScene" id=1]
[ext_resource path="res://theme/menu.tres" type="Theme" id=2]

[sub_resource type="GDScript" id=1]
script/source = "extends CanvasLayer


# Declare member variables here. Examples:
# var a = 2
# var b = \"text\"


# Called when the node enters the scene tree for the first time.
func _ready():
	get_tree().connect(\"files_dropped\", self, \"load_in\")
	
func load_in(files_dropped : PoolStringArray, screen : int):
	\"On file drop, do this\"
	print(\"{}\",files_dropped[0])

func _on_Cancel_pressed():
		get_tree().change_scene(\"res://scenes/load_cards.tscn\")
"

[node name="ImportDecks" type="CanvasLayer"]
script = SubResource( 1 )

[node name="Buttons" type="VBoxContainer" parent="."]
anchor_right = 1.0
anchor_bottom = 1.0
size_flags_horizontal = 0
theme = ExtResource( 2 )
alignment = 1

[node name="Text" type="Label" parent="Buttons"]
margin_top = 421.0
margin_right = 1920.0
margin_bottom = 572.0
size_flags_vertical = 0
theme = ExtResource( 2 )
text = "Drag and Drop to Import"
align = 1
autowrap = true

[node name="Options" type="AspectRatioContainer" parent="Buttons"]
margin_left = 861.0
margin_top = 576.0
margin_right = 1059.0
margin_bottom = 658.0
size_flags_horizontal = 4
size_flags_vertical = 0

[node name="VBoxContainer" type="VBoxContainer" parent="Buttons/Options"]
margin_right = 198.0
margin_bottom = 82.0
alignment = 1

[node name="Cancel" type="Button" parent="Buttons/Options/VBoxContainer"]
margin_right = 198.0
margin_bottom = 82.0
text = "Cancel"

[node name="transition" parent="." instance=ExtResource( 1 )]

[connection signal="pressed" from="Buttons/Options/VBoxContainer/Cancel" to="." method="_on_Cancel_pressed"]
